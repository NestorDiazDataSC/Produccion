services:
  - type: web
    name: fraud-detection-api
    runtime: python
    plan: free
    rootDirectory: fraud_detection_v1
    buildCommand: |
      apt-get update
      apt-get install -y build-essential gcc g++ python3-dev
      pip install --upgrade pip
      pip install --no-cache-dir wheel  # 🔥 Instalar wheel primero
      pip install --no-cache-dir pandas==1.5.3 --only-binary=:all:  # 🔥 Forzar wheel
      pip install --no-cache-dir -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
